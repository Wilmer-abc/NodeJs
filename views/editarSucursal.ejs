<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Sucursal</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    form {
      background-color: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 300px;
      margin: 0 auto;
    }

    label {
      display: block;
      margin-bottom: 5px;
      color: #555;
    }

    input[type="text"] {
      width: 100%;
      padding: 10px;
      margin-bottom: 15px;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-sizing: border-box;
    }

    button {
      background-color: #5c67f2;
      color: white;
      border: none;
      padding: 10px;
      border-radius: 5px;
      width: 100%;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #3f4bcd;
    }

    a {
      display: block;
      text-align: center;
      margin-top: 20px;
      color: #5c67f2;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    @media (min-width: 600px) {
      form {
        width: 400px;
      }
    }
  </style>
</head>
<body>
  <h1>Editar Sucursal: <%= sucursal.descripcion %></h1>

  <form action="/empresa/<%= idEmpresa %>/sucursal/<%= sucursal.idSucursal %>/editar" method="POST">
    <label for="descripcion">Descripción:</label>
    <input type="text" name="descripcion" value="<%= sucursal.descripcion %>" required>

    <label for="direccion">Dirección:</label>
    <input type="text" name="direccion" value="<%= sucursal.direccion %>" required>

    <label for="telefono">Teléfono:</label>
    <input type="text" name="telefono" value="<%= sucursal.telefono %>" required>

    <label for="encargado">Encargado:</label>
    <input type="text" name="encargado" value="<%= sucursal.encargado %>" required>

    <label for="estado">Estado:</label>
    <input type="text" name="estado" value="<%= sucursal.estado %>" required>

    <button type="submit">Guardar Cambios</button>
  </form>

  <a href="/empresa/<%= idEmpresa %>/sucursales">Volver a las Sucursales</a>
</body>
</html>
